{
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  },
  "scripts": {
    "watch:css": "tailwindcss -i styles.css -o dist.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
